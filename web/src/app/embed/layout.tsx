/**
 * Minimal layout for embed pages — no global nav, minimal chrome.
 * Overrides the root layout for the /embed/* subtree.
 */
export default function EmbedLayout({ children }: { children: React.ReactNode }) {
  return children;
}
